import { useState } from 'react';
import { CheckCircle2, CircleAlert, FileText, LockKeyhole, MapPin, ShieldCheck, UserRound } from 'lucide-react';
import { toast } from 'sonner';

type Step = { id: number; title: string; copy: string; icon: typeof UserRound };
const steps: Step[] = [
  { id: 1, title: 'Personal information', copy: 'Review the identity details that an authorized provider would need.', icon: UserRound },
  { id: 2, title: 'Identity document', copy: 'Prepare a valid government-issued identity document.', icon: FileText },
  { id: 3, title: 'Proof of address', copy: 'Prepare a recent document that shows your residential address.', icon: MapPin },
  { id: 4, title: 'Provider review', copy: 'Submit through a configured, authorized identity provider.', icon: ShieldCheck },
];

export default function KYC() {
  const [current, setCurrent] = useState(1);
  const [legalName, setLegalName] = useState('');
  const [country, setCountry] = useState('');
  const [acknowledged, setAcknowledged] = useState(false);
  const next = () => { if (current === 1 && (!legalName.trim() || !country.trim())) { toast.error('Add your legal name and country to continue this local preview.'); return; } if (current === 3 && !acknowledged) { toast.error('Confirm that you understand the privacy notice before continuing.'); return; } if (current < 4) { setCurrent((step) => step + 1); } else { toast.info('Identity submission is unavailable.', { description: 'No authorized verification provider is connected to this preview.' }); } };
  return <main className="min-h-screen bg-background text-foreground"><header className="border-b border-border bg-background/70 backdrop-blur-xl"><div className="container py-5 sm:py-7"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-foreground-muted">Account verification</p><h1 className="mt-1 text-2xl font-semibold tracking-tight sm:text-3xl">Identity verification</h1><p className="mt-1 text-sm text-foreground-secondary">Follow the preparation steps before starting a secure verification with an authorized provider.</p></div></header>
    <div className="container max-w-5xl py-5 sm:py-7 lg:py-8"><div className="grid gap-5 lg:grid-cols-[280px_minmax(0,1fr)]"><aside className="rounded-2xl border border-border bg-card/75 p-4 shadow-[0_20px_64px_rgba(3,7,34,0.16)] backdrop-blur-xl"><p className="text-[11px] font-semibold uppercase tracking-[0.14em] text-foreground-muted">Verification path</p><ol className="mt-3 space-y-2">{steps.map((step) => { const Icon = step.icon; const status = step.id < current ? 'done' : step.id === current ? 'current' : 'upcoming'; return <li key={step.id} className={`flex gap-3 rounded-xl p-3 ${status === 'current' ? 'bg-primary/10' : ''}`}><span className={`inline-flex size-8 shrink-0 items-center justify-center rounded-lg ${status === 'done' ? 'bg-success/10 text-success' : status === 'current' ? 'bg-primary/15 text-primary' : 'bg-background-secondary text-foreground-muted'}`}>{status === 'done' ? <CheckCircle2 className="size-4" aria-hidden="true" /> : <Icon className="size-4" aria-hidden="true" />}</span><span><span className="block text-sm font-semibold">{step.id}. {step.title}</span><span className="mt-1 block text-xs leading-5 text-foreground-secondary">{step.copy}</span></span></li>; })}</ol></aside><section className="overflow-hidden rounded-2xl border border-border bg-card/75 shadow-[0_20px_64px_rgba(3,7,34,0.16)] backdrop-blur-xl"><header className="border-b border-border p-4 sm:p-5"><p className="text-[11px] font-semibold uppercase tracking-[0.14em] text-foreground-muted">Step {current} of 4</p><h2 className="mt-1 text-lg font-semibold">{steps[current - 1]?.title}</h2><p className="mt-1 text-sm leading-6 text-foreground-secondary">{steps[current - 1]?.copy}</p></header><div className="p-4 sm:p-5">{current === 1 && <div className="space-y-4"><Field label="Legal name" id="kyc-name"><input id="kyc-name" value={legalName} onChange={(event) => setLegalName(event.target.value)} placeholder="Enter the name on your identity document" autoComplete="name" className="input" /></Field><Field label="Country of residence" id="kyc-country"><input id="kyc-country" value={country} onChange={(event) => setCountry(event.target.value)} placeholder="Country or region" autoComplete="country-name" className="input" /></Field><PrivacyNotice /></div>}{current === 2 && <DocumentPreparation title="Identity document" copy="Prepare a valid passport, national identity card, or driver’s licence. Do not upload it here: this preview does not store or transmit documents." icon={FileText} />}{current === 3 && <div className="space-y-4"><DocumentPreparation title="Proof of address" copy="Prepare a recent utility bill, bank statement, or lease agreement that matches your profile details. This preview does not accept document uploads." icon={MapPin} /><label className="flex cursor-pointer items-start gap-3 rounded-xl border border-border bg-background-secondary p-3.5"><input type="checkbox" checked={acknowledged} onChange={(event) => setAcknowledged(event.target.checked)} className="mt-0.5 size-4 accent-primary" /><span className="text-xs leading-5 text-foreground-secondary">I understand that identity documents should only be shared through a configured, authorized verification provider and never sent through regular support channels.</span></label></div>}{current === 4 && <div className="rounded-xl border border-warning/25 bg-warning/8 p-4"><div className="flex gap-3"><CircleAlert className="mt-0.5 size-5 shrink-0 text-warning" aria-hidden="true" /><div><h3 className="text-sm font-semibold text-warning">Provider connection required</h3><p className="mt-1 text-sm leading-6 text-foreground-secondary">This interface is a preparation preview. No identity-provider connection is configured, so no documents, profile data, review timeline, or approval decision can be submitted or represented here.</p></div></div></div>}<div className="mt-6 flex flex-col-reverse gap-2 border-t border-border pt-5 sm:flex-row sm:justify-between">{current > 1 ? <button type="button" onClick={() => setCurrent((step) => step - 1)} className="rounded-xl border border-border px-4 py-3 text-sm font-semibold text-foreground-secondary hover:text-foreground focus-visible:outline-none">Back</button> : <span />}{current < 4 ? <button type="button" onClick={next} className="rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground transition-transform active:scale-[0.98] focus-visible:outline-none">Continue preview</button> : <button type="button" onClick={next} className="rounded-xl border border-warning/35 bg-warning/10 px-4 py-3 text-sm font-semibold text-warning focus-visible:outline-none">Check connection status</button>}</div></div></section></div></div></main>;
}
function Field({ label, id, children }: { label: string; id: string; children: React.ReactNode }) { return <label htmlFor={id} className="block"><span className="mb-2 block text-xs font-semibold text-foreground-secondary">{label}</span>{children}</label>; }
function PrivacyNotice() { return <div className="flex gap-2 rounded-xl border border-primary/20 bg-primary/7 p-3.5"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-primary" aria-hidden="true" /><p className="text-xs leading-5 text-foreground-secondary">Entries remain in this browser preview only. Do not enter a document number, address, or other sensitive identity information unless a verified provider is connected.</p></div>; }
function DocumentPreparation({ title, copy, icon: Icon }: { title: string; copy: string; icon: typeof FileText }) { return <div className="rounded-xl border border-dashed border-border bg-background-secondary/55 p-5 text-center"><span className="inline-flex size-11 items-center justify-center rounded-xl bg-primary/10 text-primary"><Icon className="size-5" aria-hidden="true" /></span><h3 className="mt-3 text-sm font-semibold">{title}</h3><p className="mx-auto mt-1 max-w-md text-sm leading-6 text-foreground-secondary">{copy}</p><span className="mt-4 inline-flex rounded-lg border border-border px-3 py-2 text-xs font-semibold text-foreground-muted">Upload unavailable in preview</span></div>; }
