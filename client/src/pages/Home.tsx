import { Activity, ArrowUpRight, Bell, BookOpenCheck, LockKeyhole, LogOut, ShieldCheck, Sparkles } from 'lucide-react';
import { Link } from 'wouter';
import { startLogin } from '@/const';
import { useAuth } from '@/_core/hooks/useAuth';

const destinations = [
  { href: '/dashboard', label: 'Analysis workspace', copy: 'Review connected market panels, data states, and educational technical context.', icon: <Activity className="size-5" aria-hidden="true" /> },
  { href: '/alerts', label: 'Alert workspace', copy: 'Create local session alerts with their delivery and persistence boundaries made explicit.', icon: <Bell className="size-5" aria-hidden="true" /> },
  { href: '/watchlist', label: 'Research watchlist', copy: 'Maintain a local list of followed assets with clearly labeled reference values.', icon: <BookOpenCheck className="size-5" aria-hidden="true" /> },
  { href: '/markets', label: 'Markets', copy: 'Move to the current terminal market view for the established exchange experience.', icon: <Sparkles className="size-5" aria-hidden="true" /> },
];

export default function Home() {
  const { user, isAuthenticated, logout } = useAuth();
  return <main className="min-h-screen bg-background text-foreground"><section className="border-b border-border bg-[radial-gradient(circle_at_70%_-20%,rgba(115,82,255,0.20),transparent_34%)]"><div className="container max-w-6xl py-10 sm:py-14 lg:py-20"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-foreground-muted">Legacy terminal entry</p><div className="mt-4 grid gap-7 lg:grid-cols-[minmax(0,1fr)_340px] lg:items-end"><div><h1 className="max-w-3xl text-3xl font-semibold tracking-tight sm:text-5xl">A disciplined route into the <span className="text-primary">Nexus</span> trading workspace.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-foreground-secondary sm:text-lg">This retained entry point now routes to the current terminal experience. Research information is presented for education and workflow orientation; it is not investment advice, an execution instruction, or a claim of real-time market coverage.</p><div className="mt-7 flex flex-wrap gap-3"><Link href="/markets" className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground transition-transform active:scale-[0.98] focus-visible:outline-none">Explore markets<ArrowUpRight className="size-4" aria-hidden="true" /></Link><Link href="/dashboard" className="inline-flex items-center gap-2 rounded-xl border border-border bg-card/75 px-4 py-2.5 text-sm font-semibold transition-colors hover:bg-background-secondary focus-visible:outline-none">Open analysis workspace<ArrowUpRight className="size-4" aria-hidden="true" /></Link></div></div><aside className="rounded-2xl border border-border bg-card/75 p-5 shadow-[0_20px_64px_rgba(3,7,34,0.16)] backdrop-blur-xl"><div className="flex items-center gap-3"><span className="inline-flex size-9 items-center justify-center rounded-xl bg-primary/10 text-primary"><ShieldCheck className="size-5" aria-hidden="true" /></span><div><p className="text-sm font-semibold">Session boundary</p><p className="mt-0.5 text-xs text-foreground-muted">Authentication remains managed by the application shell.</p></div></div>{isAuthenticated && user ? <div className="mt-5"><p className="text-sm text-foreground-secondary">Signed in as</p><p className="mt-1 truncate font-medium">{user.name || user.email || 'Account holder'}</p><button type="button" onClick={() => void logout()} className="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-foreground-secondary transition-colors hover:text-foreground focus-visible:outline-none"><LogOut className="size-4" aria-hidden="true" />Sign out</button></div> : <div className="mt-5"><p className="text-sm leading-6 text-foreground-secondary">Sign in only when you need an authenticated workflow. Browsing this entry page does not create an account action.</p><button type="button" onClick={startLogin} className="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-primary transition-colors hover:text-primary/80 focus-visible:outline-none"><LockKeyhole className="size-4" aria-hidden="true" />Sign in securely<ArrowUpRight className="size-4" aria-hidden="true" /></button></div>}</aside></div></div></section><section className="container max-w-6xl py-8 sm:py-12"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-foreground-muted">Current workspaces</p><h2 className="mt-2 text-2xl font-semibold">Continue where the task belongs</h2></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{destinations.map((destination) => <Link key={destination.href} href={destination.href} className="group rounded-2xl border border-border bg-card/75 p-5 shadow-[0_20px_64px_rgba(3,7,34,0.16)] transition-colors hover:border-primary/35 hover:bg-background-secondary/60 focus-visible:outline-none"><span className="inline-flex size-10 items-center justify-center rounded-xl bg-primary/10 text-primary">{destination.icon}</span><div className="mt-5 flex items-start justify-between gap-3"><div><h3 className="text-base font-semibold">{destination.label}</h3><p className="mt-2 text-sm leading-6 text-foreground-secondary">{destination.copy}</p></div><ArrowUpRight className="size-4 shrink-0 text-foreground-muted transition-transform group-hover:-translate-y-0.5 group-hover:translate-x-0.5" aria-hidden="true" /></div></Link>)}</div></section><section className="border-y border-border bg-card/30"><div className="container max-w-6xl py-8 sm:py-10"><div className="grid gap-5 md:grid-cols-3"><Boundary title="Clear data states" copy="Every connected, illustrative, local, and unavailable state is identified so interface confidence remains proportional to the data source." /><Boundary title="No simulated execution" copy="Routing to a workspace does not place a trade, submit a transfer, or send an alert. Review each configured action at its destination." /><Boundary title="Security first" copy="Use configured authentication and secure account tools for sensitive tasks. Never paste trading-enabled or withdrawal-enabled credentials into a preview." /></div></div></section></main>;
}
function Boundary({ title, copy }: { title: string; copy: string }) { return <article className="rounded-2xl border border-border bg-background/60 p-5"><p className="text-sm font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-foreground-secondary">{copy}</p></article>; }
