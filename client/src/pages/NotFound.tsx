import { ArrowUpRight, Compass, Home, SearchX } from 'lucide-react';
import { Link } from 'wouter';

export default function NotFound() {
  return <main className="flex min-h-screen items-center bg-background px-4 py-10 text-foreground sm:px-6"><section className="mx-auto w-full max-w-2xl rounded-3xl border border-border bg-card/75 p-6 text-center shadow-[0_28px_90px_rgba(3,7,34,0.22)] backdrop-blur-xl sm:p-10"><span className="inline-flex size-14 items-center justify-center rounded-2xl bg-primary/10 text-primary"><SearchX className="size-7" aria-hidden="true" /></span><p className="mt-6 text-xs font-semibold uppercase tracking-[0.18em] text-foreground-muted">Workspace not found</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">This route is not available.</h1><p className="mx-auto mt-4 max-w-lg text-sm leading-7 text-foreground-secondary">The page may have moved, the address may be incomplete, or the workspace may not be configured in this release. Return to a current terminal route to continue.</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Link href="/" className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground transition-transform active:scale-[0.98] focus-visible:outline-none"><Home className="size-4" aria-hidden="true" />Return home</Link><Link href="/markets" className="inline-flex items-center justify-center gap-2 rounded-xl border border-border bg-background-secondary px-4 py-2.5 text-sm font-semibold transition-colors hover:bg-card focus-visible:outline-none"><Compass className="size-4" aria-hidden="true" />Explore markets<ArrowUpRight className="size-4" aria-hidden="true" /></Link></div></section></main>;
}
