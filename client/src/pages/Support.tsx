import { useMemo, useState } from 'react';
import { ChevronDown, HelpCircle, Mail, MessageCircle, Search, Send } from 'lucide-react';
import { toast } from 'sonner';

type FAQ = { id: string; category: string; question: string; answer: string };
const faqs: FAQ[] = [
  { id: 'trade', category: 'Trading', question: 'How do I place a trade?', answer: 'Open the Trading workspace, choose a pair and order type, enter an amount, then review the order before submitting it through a connected trading provider.' },
  { id: 'leverage', category: 'Trading', question: 'What is leverage trading?', answer: 'Leverage can amplify both gains and losses. Review margin requirements, liquidation risk, and the product disclosure before using any leveraged product.' },
  { id: 'deposit', category: 'Wallet', question: 'How do I deposit crypto?', answer: 'Open Deposit, select the exact asset and network, then copy the displayed address. Only send the selected asset using the selected network.' },
  { id: 'fee', category: 'Wallet', question: 'What are withdrawal fees?', answer: 'Network and service fees can vary by asset and network. The withdrawal page shows an estimated preview before any connected-provider submission.' },
  { id: 'twofa', category: 'Security', question: 'How do I enable 2FA?', answer: 'Open Security and follow the two-factor authentication setup guidance. Use an authenticator app and store recovery details securely.' },
  { id: 'password', category: 'Account', question: 'How do I reset my password?', answer: 'Use the secure account recovery route associated with your identity provider. Never share a password or recovery code in a support message.' },
];
const categories = ['All', ...Array.from(new Set(faqs.map((faq) => faq.category)))];

export default function Support() {
  const [view, setView] = useState<'faq' | 'contact'>('faq');
  const [open, setOpen] = useState<string | null>(null);
  const [query, setQuery] = useState('');
  const [category, setCategory] = useState('All');
  const [subject, setSubject] = useState('');
  const [topic, setTopic] = useState('Trading issue');
  const [message, setMessage] = useState('');
  const filtered = useMemo(() => faqs.filter((faq) => (category === 'All' || faq.category === category) && `${faq.question} ${faq.answer}`.toLowerCase().includes(query.toLowerCase())), [category, query]);
  const submit = () => { if (!subject.trim() || !message.trim()) { toast.error('Add a subject and message before continuing.'); return; } toast.info('Support request preview saved locally only.', { description: 'No live support channel is connected to this interface.' }); setSubject(''); setMessage(''); };
  return <main className="min-h-screen bg-background text-foreground"><header className="border-b border-border bg-background/70 backdrop-blur-xl"><div className="container py-5 sm:py-7"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-foreground-muted">Help center</p><h1 className="mt-1 text-2xl font-semibold tracking-tight sm:text-3xl">Support</h1><p className="mt-1 text-sm text-foreground-secondary">Search product guidance or prepare a support request.</p></div></header>
    <div className="container max-w-5xl py-5 sm:py-7 lg:py-8"><div className="mb-5 inline-flex rounded-xl border border-border bg-background-secondary p-1" role="tablist" aria-label="Support views">{([{ id: 'faq', label: 'Product guidance' }, { id: 'contact', label: 'Contact preview' }] as const).map((tab) => <button key={tab.id} type="button" role="tab" aria-selected={view === tab.id} onClick={() => setView(tab.id)} className={`rounded-lg px-3 py-2 text-xs font-semibold transition-colors focus-visible:outline-none sm:px-4 ${view === tab.id ? 'bg-card text-foreground shadow-sm' : 'text-foreground-secondary hover:text-foreground'}`}>{tab.label}</button>)}</div>{view === 'faq' ? <section className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_250px]"><div className="overflow-hidden rounded-2xl border border-border bg-card/75 shadow-[0_20px_64px_rgba(3,7,34,0.16)] backdrop-blur-xl"><div className="border-b border-border p-4 sm:p-5"><label htmlFor="faq-search" className="sr-only">Search questions</label><div className="relative"><Search className="absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-foreground-muted" aria-hidden="true" /><input id="faq-search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search product guidance" className="w-full rounded-xl border border-border bg-background-secondary py-3 pl-10 pr-3 text-sm outline-none transition-colors placeholder:text-foreground-muted focus:border-primary/55" /></div></div>{filtered.length ? <div className="divide-y divide-border">{filtered.map((faq) => <article key={faq.id}><button type="button" onClick={() => setOpen(open === faq.id ? null : faq.id)} aria-expanded={open === faq.id} className="flex w-full items-center justify-between gap-4 px-4 py-4 text-left transition-colors hover:bg-background-secondary/55 focus-visible:outline-none sm:px-5"><span><span className="block text-sm font-semibold">{faq.question}</span><span className="mt-1 block text-xs text-foreground-muted">{faq.category}</span></span><ChevronDown className={`size-4 shrink-0 text-foreground-muted transition-transform ${open === faq.id ? 'rotate-180' : ''}`} aria-hidden="true" /></button>{open === faq.id && <div className="border-t border-border bg-background-secondary/45 px-4 py-4 text-sm leading-6 text-foreground-secondary sm:px-5">{faq.answer}</div>}</article>)}</div> : <div className="flex min-h-64 flex-col items-center justify-center px-5 text-center"><HelpCircle className="size-8 text-foreground-muted" aria-hidden="true" /><h2 className="mt-3 text-base font-semibold">No guidance found</h2><p className="mt-1 text-sm text-foreground-secondary">Try a different phrase or category.</p></div>}</div><aside className="h-fit rounded-2xl border border-border bg-card/75 p-4 shadow-[0_20px_64px_rgba(3,7,34,0.16)] backdrop-blur-xl"><p className="text-[11px] font-semibold uppercase tracking-[0.14em] text-foreground-muted">Topic</p><div className="mt-3 flex flex-wrap gap-2">{categories.map((entry) => <button type="button" key={entry} onClick={() => setCategory(entry)} className={`rounded-lg border px-3 py-2 text-xs font-semibold transition-colors focus-visible:outline-none ${category === entry ? 'border-primary/40 bg-primary/10 text-primary' : 'border-border bg-background-secondary text-foreground-secondary hover:text-foreground'}`}>{entry}</button>)}</div></aside></section> : <section className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_250px]"><form onSubmit={(event) => { event.preventDefault(); submit(); }} className="rounded-2xl border border-border bg-card/75 p-4 shadow-[0_20px_64px_rgba(3,7,34,0.16)] backdrop-blur-xl sm:p-5"><p className="text-[11px] font-semibold uppercase tracking-[0.14em] text-foreground-muted">Contact request</p><h2 className="mt-1 text-lg font-semibold">Prepare a support message</h2><p className="mt-1 text-sm leading-6 text-foreground-secondary">This preview is not connected to a live support inbox. Do not include passwords, recovery codes, or wallet seed phrases.</p><div className="mt-5 space-y-4"><Field label="Subject" id="support-subject"><input id="support-subject" value={subject} onChange={(event) => setSubject(event.target.value)} placeholder="How can we help?" className="input" /></Field><Field label="Topic" id="support-topic"><select id="support-topic" value={topic} onChange={(event) => setTopic(event.target.value)} className="input"><option>Trading issue</option><option>Wallet issue</option><option>Security concern</option><option>Account issue</option><option>Other</option></select></Field><Field label="Message" id="support-message"><textarea id="support-message" value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Describe the problem without sensitive data." rows={6} className="input resize-y" /></Field><button type="submit" className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground transition-transform active:scale-[0.98] focus-visible:outline-none"><Send className="size-4" aria-hidden="true" />Save support preview</button></div></form><aside className="space-y-3"><InfoCard icon={MessageCircle} title="Live chat" copy="Not connected in this preview." /><InfoCard icon={Mail} title="Email support" copy="A live inbox is not configured." /></aside></section>}</div></main>;
}
function Field({ label, id, children }: { label: string; id: string; children: React.ReactNode }) { return <label htmlFor={id} className="block"><span className="mb-2 block text-xs font-semibold text-foreground-secondary">{label}</span>{children}</label>; }
function InfoCard({ icon: Icon, title, copy }: { icon: typeof Mail; title: string; copy: string }) { return <section className="rounded-2xl border border-border bg-card/75 p-4 shadow-[0_16px_48px_rgba(3,7,34,0.12)]"><Icon className="size-4 text-primary" aria-hidden="true" /><h3 className="mt-3 text-sm font-semibold">{title}</h3><p className="mt-1 text-xs leading-5 text-foreground-secondary">{copy}</p></section>; }
